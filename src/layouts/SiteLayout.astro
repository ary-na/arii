---
import BaseHead from '@components/BaseHead.astro';
import Header from '@components/Header.astro';
import Footer from '@components/Footer.astro';

interface Props {
  title: string;
  description: string;
  image?: import('astro').ImageMetadata;
  contentClass?: string;
}

const { title, description, image, contentClass } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead {title} {description} {image} />
  </head>
  <body class="flex min-h-screen flex-col">
    <Header />
    <main
      id="main-content"
      class:list={[
        'mx-auto flex w-full max-w-3xl flex-1 flex-col justify-center px-4 py-8',
        contentClass,
      ]}
    >
      <slot />
    </main>
    <Footer />
  </body>
</html>

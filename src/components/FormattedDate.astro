---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class="font-mono text-[0.8125rem] tracking-wide" datetime={date.toISOString()}>
  {
    date.toLocaleDateString('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
    })
  }
</time>
